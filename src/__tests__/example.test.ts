import { describe, expect, it } from '@jest/globals';

// Simple sanity test example – replace with real component tests later.
describe('Math sanity', () => {
  it('adds numbers correctly', () => {
    expect(1 + 2).toBe(3);
  });
});
